# 📅 Раздел «События» (Events)

**Файл:** `src/views/Events/Events.svelte`
**Маршрут:** `/events`

Основной раздел для навигации по календарю мероприятий Клуба. Позволяет просматривать расписание, фильтровать события по типам и переходить к регистрации.

| Список | Календарь |
|---|---|
| ![Events List](events-calendar-off.png) { width="300" } | ![Full Calendar](events-calendar-on.png) { width="300" } |

### 1. Структура интерфейса

Экран состоит из трех слоев взаимодействия:

#### A. Верхняя панель и Фильтры
*   **Заголовок:** «События».
*   **Кнопка Фильтров (справа):**
    *   Иконка с индикатором (бейджем) активных фильтров.
    *   *Действие:* Открывает модальное окно `Filters` (`src/views/Events/components/Filters/index.svelte`).
    *   *Стор:* Состояние фильтров хранится в `eventsFilters` (показывать календарь, показывать прошедшие события и т.д.).

#### B. Календарь (Два состояния)
В этом разделе реализован сложный UI-паттерн с использованием **CupertinoPane** для трансформации календаря:

1.  **Слайдер (Свернутое состояние):**
    *   **Компонент:** `CalendarSlider`.
    *   Отображает горизонтальную ленту дат.
    *   Цветные точки под датами индицируют наличие событий определенных форматов (цвета берутся из `src/config/events.cfg.ts`).
2.  **Полный календарь (Развернутое состояние):**
    *   **Компонент:** `Calendar` внутри `CupertinoPane` (`#events-swiper`).
    *   Открывается свайпом вниз или кликом по области календаря.
    *   Позволяет быстро переключаться между месяцами.

#### C. Список событий
*   **Компонент:** `EventCard`.
*   **Логика:** Список реактивно обновляется при выборе даты в календаре или изменении фильтров.
*   **Группировка:** Если выбрана опция "Показывать ближайшие", список может содержать разделитель (например, "Ближайшие события" для `id: -1`).

---

### 2. Техническая реализация

#### Data Flow
Загрузка данных управляется через Entity/Collector паттерн:
*   **Query:** `eventsFeed` (`src/queries/event.ts`).
*   **Параметры:** `from`, `to` (диапазон дат, зависящий от текущего вида календаря).
*   **Кэширование:** Полученные события группируются по датам в локальном объекте `events` (ключ — дата в формате строки), чтобы избежать повторных запросов при переключении дней.

#### Конфигурация форматов (`events.cfg.ts`)
Внешний вид карточек и индикаторов в календаре полностью зависит от конфигурации.

Пример структуры конфига:
```typescript
export const EVENTS = [
    {
        format: 'tender',
        name: 'Тендерный разбор',
        color: 'primary brightness-90', // CSS классы Tailwind/DaisyUI
        icon: '<svg...>' // SVG строка
    },
    // ...
];
```

#### Навигация
При клике на карточку события происходит переход на детальную страницу:
*   **Маршрут:** `/events/:id`
*   **Компонент:** `src/views/Event/Event.svelte`

---

### 3. Компоненты раздела

| Компонент | Путь | Описание |
| :--- | :--- | :--- |
| **Calendar** | `views/Events/components/Calendar` | Полная сетка месяца. Логика генерации дней (`createCalendarRange`). |
| **CalendarSlider** | `views/Events/components/CalendarSlider` | Горизонтальный свайпер (Swiper.js) для выбора дат в свернутом режиме. |
| **EventCard** | `views/Events/components/EventCard` | Карточка превью. Отображает дату, иконку формата, название и фоновое изображение. |
| **Filters** | `views/Events/components/Filters` | Модальное окно настроек отображения (показывать ли календарь при входе, показывать ли прошедшие). |

---

### 4. Особенности поведения (UX)

1.  **Синхронизация:** При выборе даты в полном календаре, слайдер автоматически прокручивается к выбранной дате (`calendarSlider.setActiveDate`).
2.  **Cupertino Pane:** Использование этой библиотеки позволяет создать "нативный" опыт iOS шторки для календаря, которая плавно перекрывает контент.
3.  **Обработка изображений:** В `EventCard` реализован фоллбэк: если изображение события не загружается (`handleBgImageError`), подставляется дефолтная заглушка.
