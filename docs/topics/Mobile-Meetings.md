# 🤝 Раздел «Встречи и контакты» (Meetings)

**Файл:** `src/views/Meetings/Meetings.svelte`
**Маршрут:** `/meetings`

Единый центр управления коммуникациями резидента. Позволяет отслеживать календарь встреч, управлять входящими запросами и вести историю контактов.

| Назначенные | Запрошенные (пустой список) |
|---|---|
| ![Scheduled Meetings](meetings-scheduled.png) { width="300" } | ![Requested Meetings](meetings-requested.png) { width="300" } |

## 1. Режимы отображения (Tabs)

Экран разделен на три вкладки, переключаемые в верхней части:

### A. Назначенные (Scheduled)
*   **Состояние по умолчанию.**
*   Отображает список подтвержденных встреч (онлайн и офлайн) и фактов обмена контактами.
*   **Карточка встречи:**
    *   **Дата и время:** Выделены синим бейджем (например, "29 ноября").
    *   **Собеседник:** Аватар, ФИО, Должность и Компания.
    *   **Статус:** Зеленая галочка на аватаре означает подтвержденный статус или успешный контакт.
    *   **Действия:** Ряд кнопок внизу карточки для быстрого взаимодействия (Оценить, Перейти в профиль, Чат).

### B. Запрошенные (Requested)
*   Содержит входящие и исходящие запросы на онлайн-встречи, ожидающие согласования времени.
*   **Индикатор:** Красный бейдж на вкладке показывает количество активных запросов (синхронизировано со стором `bindings`).
*   **Логика согласования:** Инициатор предлагает 3 слота времени, получатель выбирает один или отклоняет.
*   *Если список пуст:* Отображается чистый белый экран.

### C. Архив (Archive)
*   История завершенных встреч.
*   Доступен фильтр **"Показать только без оценки"** (чекбокс с зеленой галочкой), чтобы быстро найти встречи, требующие фидбэка.

---

## 2. Элементы интерфейса

### Карточка Встречи (`ResidentCard`)
Визуальное представление участника и контекста встречи:
*   **Индикатор "ЗНАКОМЫ":** Яркий зеленый бейдж под аватаром. Появляется, если контакт уже был установлен ранее.
*   **Контекст:** Поле "Тема встречи" отображает цель общения.
*   **Оценка:** Блок обратной связи с кнопками (Звезда, Лайк/Дизлайк) для оценки качества контакта.

### Нижняя навигация (Bottom Bar)
Сквозной элемент управления приложением:
*   **Главная:** Домик (переход на `/home`).
*   **События:** Календарь (переход на `/events`).
*   **AI:** Робот (Ассистент/Мэтчинг).
*   **Встречи:** Люди (активный раздел, отображает бейдж с количеством уведомлений).
*   **Профиль:** Человечек (переход на `/me`).
*   **Чат:** Сообщение (центр переписки).

---

## 3. Техническая реализация

### Data Flow
Используется **единый агрегирующий запрос** для получения всех типов связей:
*   **Query:** `userEventsConnectionsAll` (`src/queries/user.ts`).
*   **Параметры:** `archive` (boolean), `requests` (boolean), `dt` (текущая дата).

### Клиентская фильтрация (`parseData`)
Полученные данные распределяются по визуальным группам на основе флага `state` (состоялась):
*   Если `state == true` → Встреча попадает в **Архив**.
*   Если `state == false | null` → Встреча считается **Назначенной** (предстоящей).

### Обработка статусов (Цветовое кодирование)
*   🟢 **Зеленый:** Согласовано / Состоялась.
*   🟡 **Желтый:** Ожидает подтверждения / В пути.
*   🔴 **Красный:** Отклонено / Не состоялась.
