# 🤝 Раздел «Встречи и контакты» (Meetings)

**Файл:** `src/views/Meetings/Meetings.svelte`
**Маршрут:** `/meetings`

Центр управления коммуникациями. Здесь собираются запланированные онлайн-встречи, встречи в рамках офлайн-мероприятий и история обмена контактами.

| Назначенные встречи | Запрошенные |
|---|---|
| ![Scheduled Meetings](meetings-scheduled.png) { width="300" } | ![Requested Meetings](meetings-requested.png) { width="300" } |

## 1. Режимы отображения (Tabs)

Экран работает в трех режимах, переключаемых верхней панелью:

### A. Назначенные (Scheduled)
*   **Активен по умолчанию.**
*   **Контент:** Подтвержденные предстоящие встречи (онлайн и офлайн) + несостоявшиеся (отмененные).
*   **Сортировка:** Хронологическая.

### B. Запрошенные (Requested)
*   **Контент:** Входящие и исходящие запросы на онлайн-встречи, требующие согласования времени.
*   **Индикация:** Красный бейдж на кнопке показывает количество активных запросов (из стора `bindings`).
*   **Логика согласования:**
    *   Инициатор предлагает **3 слота времени**.
    *   Получатель выбирает один из них или отклоняет запрос.

### C. Архив (Archive)
*   **Контент:** История успешно завершенных встреч и обменов контактами.
*   **Фильтр "Без оценки":** Специальный тоггл-кнопка (звездочка), позволяющий отфильтровать встречи, по которым пользователь еще не оставил обратную связь (рейтинг/комментарий).

---

## 2. Типы Карточек

В зависимости от типа взаимодействия рендерится специфический компонент:

| Компонент | Описание |
| :--- | :--- |
| **OnlineCard** | Карточка предстоящей онлайн-встречи. Содержит время начала и кнопку перехода к звонку (если ссылка доступна). |
| **EventCard** | Группирует встречи, привязанные к конкретному офлайн-мероприятию. Внутри использует `ResidentCard`. |
| **OfflineCard** | Цифровая визитка. Фиксирует факт обмена контактами через QR-код или профиль. |
| **RequestCard** | Карточка согласования. Позволяет выбрать один из предложенных слотов времени (`userMeetingRequestConfirmation`). |

---

## 3. Техническая реализация

### Data Flow
Используется **единый агрегирующий запрос** для получения всех типов связей, что исключает рассинхрон данных между табами.

*   **Query:** `userEventsConnectionsAll` (`src/queries/user.ts`).
*   **Параметры:**
    *   `archive`: boolean — переключает выборку на исторические данные.
    *   `requests`: boolean — переключает на выборку запросов.
    *   `dt`: текущая дата (YYYY-MM-DD) для отсечения прошедших событий на бэкенде.

### Клиентская фильтрация (`parseData`)
Полученные сырые данные (`data.events`, `data.offline`, `data.online`) проходят через функцию `parseData`, которая распределяет их по визуальным группам.

> **Важно:** Встреча считается "Архивной", если её флаг `state` (состоялась) равен `true`. Если `state` равен `false` или `null` (не состоялась/планируется), она попадает в "Назначенные".

### Оценка встреч (Feedback Loop)
Реализована через модальное окно `ModalConfirmation`:
1.  Пользователь выбирает оценку (1-3 звезды, смайлики).
2.  Может выбрать готовый тег (например, "Всё прошло отлично") или написать свой комментарий.
3.  Отправляется запрос `*ConnectionMark`.

---

## 4. Особенности UX

*   **Индикатор "ЗНАКОМЫ":** Зеленый бейдж на аватаре собеседника. Появляется, если `meetings[userId]` возвращает `true` (проверка истории взаимодействий).
*   **Статусы (Цветовое кодирование):**
    *   🟢 Зеленый: Согласовано / Состоялась.
    *   🟡 Желтый: Ожидает подтверждения / В пути.
    *   🔴 Красный: Отклонено / Не состоялась.