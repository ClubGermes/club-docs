# 💎 Hidden Gems & Technical Debt

Раздел для разработчиков, описывающий неочевидные решения, хардкод и места, требующие особого внимания.

## 1. Хардкод прав доступа (Hardcoded User IDs)

В коде присутствует проверка прав доступа не только по ролям, но и **напрямую по ID пользователей**. Это критический технический долг.

*   **Файл:** `src/views/Home.svelte`
    *   `ifMegaAccess`: ID `10069`, `8000`, `10298`, `10170` имеют доступ к отчетам и отправке ИИ.
    *   `ifBossAccess`: ID `10069`, `10170` (помимо админов).
    *   Переменная `organizer`: ID `10094`.
*   **Файл:** `src/views/Users.svelte`
    *   Логика `ifAccessToUpdateRating`: ID `10069` имеет особые права на обновление рейтингов.

**Риск:** При смене ID пользователей в базе данных или увольнении сотрудников эти проверки перестанут работать или дадут доступ не тем людям.

## 2. Кастомный Router

Используется самописный роутер в `src/libs/Router`.
*   **Особенность:** Он использует `History API`, но логика парсинга параметров (особенно Query Params и регулярных выражений в путях) реализована вручную.
*   **Риск:** Может не поддерживать стандартные фичи (nested routes, lazy loading) так, как это делают стандартные библиотеки, и сложен в отладке для новых разработчиков.

## 3. XMLHttpRequest вместо Fetch

В `src/helpers/request.ts` используется `XMLHttpRequest`.
*   Это устаревший подход. Он не поддерживает `Promises` нативно (хотя обернут в Promise вручную).
*   Обработка ошибок реализована через кастомные коды (`timeoutException`, `abortException`).

## 4. "Entity" Pattern

Вся работа с данными идет через связку `Entity` + `Collector`.
*   Это создает неявную очередь запросов.
*   Данные не возвращаются напрямую из функции вызова (как `await fetch()`), а обновляют реактивную переменную внутри `Entity`. Это "Reactive" подход, но реализованный вручную.

## 5. Префиксы в LocalStorage

Для работы с локальным хранилищем используется хелпер `src/helpers/storage.ts`, который добавляет префикс `___` ко всем ключам и сериализует типы данных (сохраняет тип `number`, `boolean` и т.д. в строке вида `number|123`).
