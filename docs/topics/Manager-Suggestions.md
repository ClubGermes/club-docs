# 🔗 Менеджер: Совпадения тегов (Suggestions)

**Код:** `Suggestions.svelte` (frontend/club-manager/src/views/Suggestions.svelte)  
**Роут:** `/suggestions` (Layout: `Main`)

Модуль **Совпадения тегов** автоматически находит потенциально полезные знакомства между резидентами на основе пересечения их тегов (Сфера деятельности <-> Потребности). Менеджер использует этот инструмент для проактивного предложения знакомств (интродукций).

![Совпадения тегов](../images/manager/suggestions/suggestions_list.png)
{style="block"}

## 1. Панель фильтров

Инструменты для сегментации огромного массива совпадений (на скриншоте > 6000).

| Элемент | Описание | Привязка к коду |
| :--- | :--- | :--- |
| **Менеджер** | Фильтр по ответственному КМ. | `filter.communityManager` |
| **Поиск** | Поиск по имени или компании клиента. | `filter.name` |
| **Оценка** | Фильтр по дате последней оценки (3/7/30 дней). | `filter.dateEvaluation` |
| **Статусы** | Три кнопки (2/1/0) фильтруют по оценке "качества" совпадения. | `filter.evaluation` |

> **Индикаторы качества (Rating):**
> *   🔴 **0** — Плохое совпадение (или отказ).
> *   🟡 **1** — Среднее / Возможно интересно.
> *   🟢 **2** — Отличное совпадение (рекомендовано к знакомству).

## 2. Список совпадений

Основная область отображает пары "Клиент 1 <-> Клиент 2", у которых найдены пересечения интересов.

### Карточка совпадения

Каждая строка представляет собой **пару** участников.

1.  **Участник 1 (Слева):** Инициатор потенциального интереса (обычно тот, у кого есть "Потребность").
2.  **Участник 2 (Справа):** Потенциальный партнер (тот, у кого есть "Компетенция").
    *   *Индикаторы:* Иконки (Телефон, Календарь) показывают, были ли у них уже контакты или встречи.
3.  **Менеджеры (Центр):** Имена менеджеров обоих участников.
4.  **Оценка (Центр):** Кружок с цветной обводкой показывает текущий статус проработки этого совпадения.
    *   Клик по иконке карандаша открывает детальный просмотр.
5.  **Контекст (Справа):** Причина совпадения (совпавшие теги, дата обновления профиля).

## 3. Детальная проработка (Drawer)

При клике на карандаш открывается боковая панель с деталями совпадения.

![Детали совпадения](../images/manager/suggestions/suggestion_detail.png)
{style="block"}

### Интерфейс модерации

1.  **Карточки участников:** Полная информация о каждом (ФИО, Компания, Менеджер).
    *   Кнопки перехода в профиль.
2.  **Причина совпадения:** Явно подсвечиваются теги, которые вызвали match (например, *Юридические услуги*).
3.  **Оценка (Rating):**
    *   Менеджер может проставить оценку перспективности знакомства для **каждой стороны** отдельно (левая и правая колонки радио-кнопок).
    *   🔴 Нет интереса / 🟡 Возможно / 🟢 Интересно.
4.  **Комментарии:** Чат для менеджеров, чтобы обсудить нюансы ("Уже знакомы", "Стоит предложить на форуме" и т.д.).

### Автоматический AI-анализ

В некоторых случаях (если включено) система показывает блок **AI Rating** (`AIRating` компонент), где искусственный интеллект дает свой прогноз успешности знакомства. Менеджер может подтвердить или опровергнуть его.

## 4. Техническая реализация

### API Запросы
*   **Список:** `suggestionsList` (Endpoint: `/ma/suggestions/control`).
    *   Возвращает список пар (`suggestions`) и словарь комментариев (`comments`).
*   **Управление:**
    *   `userSuggestionsState`: Изменение статуса (интересно/нет).
    *   `userSuggestionsComment`: Добавление комментария.

### Логика отображения тегов
Функция `parseSuggestionTags` отвечает за красивый вывод совпавших тегов, разбирая строки вида `Category : Tag`.

### Особенности
*   **Двусторонняя связь:** Совпадение всегда двустороннее, но интерфейс позволяет оценить интерес каждой стороны независимо (например, Клиенту А интересен Клиент Б, но не наоборот).
*   **История:** Вся история оценок и комментариев сохраняется и доступна при повторном открытии.
