# 👥 Раздел «Резиденты» (Residents)

**Файл:** `src/views/Residents/Residents.svelte`
**Маршрут:** `/residents`

Каталог участников клуба. Инструмент для поиска бизнес-партнеров по компетенциям, просмотра рейтинга спикеров и управления записной книжкой.

| Резиденты | Спикеры | Профиль |
|---|---|---|
| ![Residents List](residents-residents.png) { width="200" } | ![Speakers List](residents-speakers.png) { width="200" } | ![Resident Profile](residents-residents-resident.png) { width="200" } |

## 1. Режимы отображения

Переключаются иконками под строкой поиска.

### A. Режим "Наш клуб" (Резиденты)
*   **Состояние по умолчанию.** Отображает полный список членов клуба.
*   **Карточка резидента (`ResidentCard`):**
    *   **Статус:** Галочка (верифицирован) или иконка медали (Золотой/Серебряный статус).
    *   **Инфо:** ФИО, Должность, Компания.
    *   **Отрасли:** Теги сфер деятельности (выделены синим).
    *   **Индикатор "ЗНАКОМЫ":** Зеленый бейдж. Появляется автоматически, если в системе зафиксирована встреча или контакт с этим пользователем.
*   **AI-подбор:** Плавающая кнопка (вверху или внизу списка), запускающая `SwipeDeck` — интерфейс для быстрого отбора рекомендованных партнеров.

### B. Режим "Спикеры"
*   **Активация:** Кнопка «Спикеры» в хедере (`filters.speakers = true`).
*   **Контент:** Участники, имеющие опыт выступлений на мероприятиях.
*   **Сортировка:** Кнопка позволяет сортировать список по 3 критериям: *Имя*, *Рейтинг*, *Количество отзывов*.
*   **Карточка спикера (`SpeakerCard`):**
    *   Отображает средний рейтинг (желтый круг, например `5.0`).
    *   Кнопка **"Оцените спикеров!"** разворачивает список прошедших выступлений для выставления оценки.

---

## 2. Поиск и Фильтрация

### Умный поиск
Поле ввода фильтрует список одновременно по **Имени** и **Названию компании**.
*   *Техническая деталь:* Используется `debounce` (задержка 250мс) для оптимизации производительности при вводе.

### Фильтры (Модальное окно)
Кнопка настроек справа открывает панель критериев:

| Иконка | Название | Логика (`residentsFilters`) |
| :--- | :--- | :--- |
| 👍 | **Полезные ответы** | Участники с рейтингом полезности > 0. |
| 🤝 | **Совпадение компетенций** | Теги пользователя (`tags`) пересекаются с вашими потребностями. |
| 🤝 | **Совпадение интересов** | Ваши компетенции пересекаются с запросами пользователя (`interests`). |
| ⭐ | **В контактах** | Пользователи, добавленные вами в избранное. |
| ☑️ | **Только все совпадения** | **Strict Mode:** показывает только тех, кто соответствует *всем* выбранным галочкам одновременно. |

![Filters Modal](residents-residents-filter.png) { width="300" }

---

## 3. Профиль резидента

Детальная карточка пользователя (`/residents/:id`).

**Структура экрана:**
1.  **Хедер:** Фото, статус в клубе, кнопки действий:
    *   *В избранное* (добавить в контакты).
    *   *Встречи* (посмотреть историю встреч с этим человеком).
2.  **Основное:** ФИО, Должность, Компания.
3.  **Метрики:**
    *   Статус (Бронза/Серебро/Золото).
    *   Годовой оборот и кол-во сотрудников (может быть скрыто настройками приватности).
    *   Стаж в бизнесе.
4.  **Компетенции (Теги):**
    *   Разделены на блоки: *Специализация компании*, *Потребности*, *Личная экспертиза*, *Хобби*.
    *   Зеленым цветом подсвечиваются теги, совпадающие с профилем смотрящего (Smart Matching).

---

## 4. Особенности UX

*   **Навигация:** При возврате из профиля в список скролл остается на прежнем месте (стор `residentsY`).
*   **Защита данных:** Для пользователей с ограниченными правами (гости) часть полей (оборот, телефон) заменяется на "не указано" или скрывается.
*   **Кэширование:** Список резидентов загружается один раз (`residentsListHandler`) и фильтруется на клиенте для мгновенного отклика.