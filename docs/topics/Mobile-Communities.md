# 💬 Раздел «Коммуникации» (Communities)

**Файл:** `src/views/Communities/Communities.svelte`
**Маршрут:** `/communities`

Центральный хаб для общения внутри Клуба. Здесь осуществляется доступ к тематическим чатам, опросам и прямая связь с персональным менеджером.

![Communities Screen](communities.png) { width="300" }

## 1. Структура интерфейса

Экран вертикально разделен на три функциональные зоны:

### A. Поиск
*   **Поле ввода:** Фильтрует список сообществ по названию в реальном времени.
*   **Логика:** Использует `debounce` (250мс) для оптимизации поиска. Поиск осуществляется как по родительским категориям, так и по вложенным чатам.

### B. Блок «Ваш комьюнити-менеджер»
Персональная карточка для быстрой связи с закрепленным менеджером.
*   **Данные:** Загружаются через `userCommunityManager` (`src/queries/user.ts`).
*   **Элементы:**
    *   Фото (при клике открывается `ModalPhoto`).
    *   ФИО и телефон (ссылка `tel:`).
    *   Кнопка **"Отправить сообщение"**: Открывает диалог в Telegram (`sendTelegramMessage`).

### C. Список Сообществ
Динамический список доступных каналов коммуникации.
*   **Компонент:** `CommunityCard` (`src/views/Communities/components/CommunityCard/`).
*   **Сортировка (`communitiesSorting`):** Умная сортировка, поднимающая наверх наиболее актуальные чаты:
    1.  Есть открытые вопросы (неотвеченные).
    2.  Есть непрочитанные сообщения.
    3.  По времени последнего сообщения (новые выше).
*   **Индикаторы:**
    *   Счетчик вопросов (например, `🗨 1 вопрос`).
    *   Бейджи новых сообщений (зеленый/синий кружки).

---

## 2. Типы Сообществ

На скриншоте представлены два основных типа, которые технически являются объектами `Community`:

1.  **"Задать вопрос Клубу"**
    *   Специализированное сообщество для Q&A.
    *   Позволяет создавать новые темы (посты), на которые отвечают эксперты или другие участники.
2.  **"Опросы клуба"**
    *   Раздел, агрегирующий активные голосования.

---

## 3. Техническая реализация

### Data Flow
Загрузка данных происходит параллельно через `collector`:

1.  **Менеджер:** Запрос `userCommunityManager`.
2.  **Список:** Запрос `communitiesList`.
    *   Возвращает иерархическую структуру (категории -> чаты).
    *   Агрегирует статистику (`subjects_open`, `answers_new`) для родительских категорий.

### Навигация
*   Клик по карточке сообщества ведет на детальную страницу чата: `/communities/:id`.
*   Если у сообщества есть вложенные под-чаты (children), карточка раскрывается (аккордеон), показывая список внутри.

---

## 4. Особенности UX

*   **Приоритезация внимания:** Алгоритм сортировки гарантирует, что пользователь в первую очередь увидит те чаты, где требуется его внимание или где есть активность (ответы на его вопросы).
*   **Прямой контакт:** Блок менеджера всегда закреплен сверху (под поиском), обеспечивая доступ к поддержке в один клик.
