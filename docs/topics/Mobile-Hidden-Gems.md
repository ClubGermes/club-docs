# 💎 Подводные камни (Mobile)

## 1. Хардкод ID пользователей

В мобильном приложении также встречается логика, завязанная на конкретные ID пользователей базы данных.

*   **Пример:** `src/views/Event/components/UserCard/index.svelte`
    *   Запрещен переход в профиль пользователя с ID `12114`.
*   **Пример:** `src/views/Polls.svelte`
    *   Особые права доступа для ID `10069` и `8000`.

## 2. Platform Specific Logic (Специфика платформ)

В коде активно используются проверки типа платформы через плагины Capacitor:
*   **Safe Area:** Отступы под «челки» и системные элементы управления рассчитываются только для `ios` и `android`. В веб-версии они игнорируются.
*   **QR Scanner:** Нативная камера для сканирования QR-кодов работает только на устройствах.

## 3. Имитация нативности (Cupertino Pane)

Для создания «шторок» (Bottom Sheets) используется библиотека `cupertino-pane`.
*   Это **не нативный** компонент системы, а JS-эмуляция.
*   Может вести себя нестабильно при сложных жестах скролла внутри шторки.

## 4. OTA Updates (Capgo)

Проект настроен на получение обновлений бандла (JS/CSS) в обход магазинов приложений.
*   Конфиг: `capacitor.config.ts`.
*   Сервер обновлений: `http://ota.grizodubov.ru:5173`.
*   **Важно:** Это означает, что версия приложения в App Store может сильно отличаться от того, что видит пользователь по факту.

## 5. XMLHttpRequest

Использование `XMLHttpRequest` в мобильном приложении вместо `fetch` является техническим долгом, унаследованным от общей кодовой базы с менеджером.
