# 💎 Подводные камни и Особенности

## 1. Хардкод ID пользователей

В клиентском приложении, как и в менеджере, есть логика, завязанная на конкретные ID.

*   **Файл:** `src/views/Event/components/UserCard/index.svelte`
    ```javascript
    /* Блокировка перехода в профиль для конкретного пользователя */
    on:click="{() => { if (user.id != 12114) router.go('/residents/' + user.id.toString()); }}"
    ```
*   **Файл:** `src/views/Polls.svelte`
    *   Проверка прав доступа для ID `10069` и `8000` (суперадмины/разработчики?).

## 2. Platform Specific Logic

В `src/App.svelte` есть проверки платформы через Capacitor Device плагин:
*   Определение Safe Area (отступов) работает только если `platform == 'ios' || platform == 'android'`.
*   Логика сканера штрих-кодов также обернута в проверку платформы. В браузере сканер работать не будет.

## 3. Особенности UI (Cupertino Pane)

Используется библиотека `cupertino-pane` для создания шторок (как в iOS).
*   Это не нативный компонент, а JS-эмуляция.
*   Может конфликтовать с глобальным скроллом страницы. Обратите внимание на классы `.pane` в `style.css`.

## 4. OTA Updates (Capgo)

В `capacitor.config.ts` прописан ключ для `CapacitorUpdater`.
*   `autoUpdate: true` (вероятно, по умолчанию).
*   URL обновлений: `http://ota.grizodubov.ru:5173`.
*   **Риск:** Если сервер OTA недоступен, приложение может долго пытаться проверить обновление при старте.

## 5. XMLHttpRequest

Использование `XMLHttpRequest` в 2025 году (в файле `src/helpers/request.ts`) — архаизм. В среде Capacitor (WebView) `fetch` работает нативно и более эффективно. Рекомендуется рефакторинг.