# 📈 Менеджер: Статистика (Воронка продаж)

**Код:** `Charts.svelte` (frontend/club-manager/src/views/Charts.svelte)  
**Роут:** `/charts` (Layout: `Main`)

## Назначение
Экран предназначен для аналитики движения клиентов по воронке продаж (стадиям членства) и анализа эффективности работы менеджеров. Он позволяет визуализировать динамику изменений и выявлять "узкие места" в процессе продаж.

> **Визуализация UI**

![Statistics Dashboard](../images/manager/statistics/manager-statistics-main.png)
{style="block"}

---

## Элементы интерфейса

### Панель фильтров
Управляет выборкой данных для построения графиков.

#### Выбор менеджера
Выпадающий список для фильтрации статистики по конкретному сотруднику.

*   **Все менеджеры** (`id: -1`): Показать статистику по всем менеджерам.
*   **Нет менеджера** (`id: 0`): Клиенты без назначенного менеджера.
*   **Список менеджеров**: Динамически загружается из `data.community_managers`. Для менеджеров Отдела Продаж добавляется метка `(ОП)`.

#### Тип менеджера (Тогглы)
Круглые кнопки-фильтры для быстрого переключения отображения по департаментам:

*   **КМ** (Синий, `border-secondary`): Community Manager (department = 1). При активации показывает только КМ.
*   **ОП** (Бирюзовый, `border-info`): Отдел Продаж (department ≠ 1). При активации показывает только ОП.
*   **НЕТ** (Красный, `border-error`): Пользователи без назначенного менеджера. При активации показывает клиентов без менеджера.

> **Примечание:** Тогглы работают как включатели — можно комбинировать несколько активных фильтров одновременно.

#### Период (Интервалы)
Круглые кнопки для выбора временного диапазона:

*   **?** (Фиолетовый, `border-primary`): Кастомный диапазон. При активации появляются два поля `DateInput` для выбора начальной и конечной даты.
*   **1** (Серый, `border-neutral`): День. Показывает последние 5 или 7 дней.
*   **7** (Серый, `border-neutral`): Неделя (активен по умолчанию). Показывает последние 5 или 7 недель.
*   **30** (Серый, `border-neutral`): Месяц. Показывает последние 5 или 7 месяцев.

#### Навигация по времени
*   **Стрелки влево (<<)**: Сдвигает временной интервал в прошлое (`filterOffset--`).
*   **Кнопка +/-**: Переключает количество отображаемых интервалов между 5 и 7 (`filterAmount`).
*   **Стрелки вправо (>>)**: Сдвигает временной интервал в будущее (`filterOffset++`). Кнопка неактивна, если `filterOffset == 0` (текущий период).

> **Важно:** Навигация по времени доступна только для стандартных периодов (день/неделя/месяц), но не для кастомного диапазона.

#### Обновить
Кнопка с иконкой обновления в правом верхнем углу. Принудительно перезагружает данные с сервера через `get()`.

### Графики (Charts)

Используется компонент `Chart.svelte` (обертка над `Chart.js`).

#### График "Все стадии" (Line Chart)
**Отображается:** Только при стандартных периодах (день/неделя/месяц), скрыт для кастомного диапазона.

Многострочный линейный график, показывающий динамику всех стадий воронки одновременно.
*   **Ось X:** Временные интервалы (например: "01/01 - 07/01" для недель).
*   **Ось Y:** Количество пользователей (автоматическое масштабирование).
*   **Легенда:** Справа от графика. Каждая стадия имеет свой цвет и название.
*   **Интерактивность:** Tooltip при наведении показывает точные значения.

#### График "Текущие" (Current Bar Chart)
**Отображается:** Всегда (для всех типов периодов).

Столбчатая диаграмма текущего состояния воронки. Показывает распределение пользователей по стадиям **на данный момент** (не в выбранном периоде, а актуальное состояние `membership.stage`).

*   **Ось X:** Названия стадий (1-6, без стадии "У агента").
*   **Ось Y:** Количество пользователей.
*   **Цвета:** Каждый столбец окрашен в цвет соответствующей стадии.
*   **Интерактивность:** Клик по столбцу выделяет его голубым цветом и отображает список пользователей справа.

#### График кастомного периода (Custom Bar Chart)
**Отображается:** Только при выборе кастомного диапазона дат (кнопка "?").

Аналогичен графику "Текущие", но показывает пользователей, которые **находились** на каждой стадии **хотя бы раз** в выбранном кастомном диапазоне дат.

*   **Заголовок:** Динамически формируется из выбранных дат (например: "01/01 - 15/01").
*   **Логика:** Берется первый интервал (`n == 0`) из `createIntervals` и собирает уникальных пользователей.

#### Детальные графики по стадиям (Bar Charts)
**Отображается:** Только при стандартных периодах (день/неделя/месяц).

Семь отдельных столбчатых диаграмм (по одной на каждую стадию 0-6). Каждый график показывает количество пользователей, которые **находились** на этой стадии в конкретный временной интервал.

*   **Заголовок:** Название стадии (например: "Адмиссия").
*   **Ось X:** Временные интервалы (совпадают с графиком "Все стадии").
*   **Ось Y:** Количество пользователей (автоматическое масштабирование).
*   **Цвета:** Светло-зеленые столбцы с зеленой обводкой по умолчанию.
*   **Интерактивность:** Клик по столбцу выделяет его голубым и показывает список пользователей справа.

---

## Бизнес-логика и Стадии

Стадии воронки жестко закодированы в системе (ID 0-6). Каждая стадия имеет свой фиксированный цвет для графиков (`CHART_COLORS`):

| ID | Название | Цвет | Описание |
| :--- | :--- | :--- | :--- |
| **0** | У агента | 🔴 Красный | Лид передан агенту |
| **1** | Соискатель | ⚫️ Темно-серый | Заполнил первичную анкету |
| **2** | Адмиссия | 🟡 Желтый | Проходит проверку |
| **3** | Подключение | 🔵 Голубой | Оформление документов |
| **4** | Кандидат | 🔵 Синий | Утвержден, ожидает оплаты |
| **5** | Оплата | 🟢 Светло-зеленый | Процесс оплаты |
| **6** | Член клуба | 🟢 Зеленый | Действующий резидент |

---

## Техническая реализация

### Архитектура данных (Client-Side Aggregation)
Экран работает по принципу **толстого клиента**.

1.  **Запрос:** Entity `userMonitorSales` (`src/queries/user.ts`) загружает сырой массив логов (`log`) и пользователей (`users`).
    *   **Эндпоинт:** `/ma/user/monitor/sales`.
    *   **Параметры:** `filter` (список полей), `showMeetingsAmount: true`.
    *   **Возврат:** `{ log: [], users: {}, community_managers: [] }`.

2.  **Агрегация:** Функция `createMonitor(intervals, log)` на клиенте обрабатывает эти данные.
    *   **Фильтрация:** Сначала применяются фильтры по менеджерам и департаментам (`itemsPrefiltered`).
    *   **Группировка:** Для каждой стадии (0-6) и каждого временного интервала подсчитывается количество уникальных пользователей.
    *   **Дедупликация:** Используется кэш `lstCache` для исключения дубликатов пользователей в одном интервале.
    *   **Формирование датасетов:** Создаются конфигурации для всех графиков (`configs`, `mergeConfig`, `currentConfig`, `customConfig`).
    *   *Риск:* При большом объеме данных (тысячи логов) пересчет может занимать время и блокировать UI.

3.  **Реактивность:** Svelte автоматически пересчитывает графики при изменении фильтров.
    *   `$: intervals = createIntervals(filterIntervals, filterAmount, filterOffset, filterDates)`
    *   `$: intervals, log, filter, createMonitor(intervals, log)`
    *   Изменение любого фильтра триггерит пересчет **без повторного запроса к API**.

### Функция createIntervals
Генерирует массив временных интервалов на основе выбранного типа периода.

**Параметры:**
*   `f`: Тип периода (`'day'`, `'week'`, `'month'`, `'custom'`).
*   `am`: Количество интервалов (5 или 7).
*   `os`: Смещение (`filterOffset`, отрицательное число для прошлого).
*   `fd`: Массив дат для кастомного диапазона `[dateStart, dateEnd]`.

**Возврат:** Массив объектов `{ min: timestamp, max: timestamp, label: string }`.

**Логика:**
*   **День:** Последние `am` дней от сегодня. Метка: `"01/01"`.
*   **Неделя:** Последние `am` недель от начала текущей недели (понедельник). Метка: `"01/01 - 07/01"`.
*   **Месяц:** Последние `am` месяцев от начала текущего месяца. Метка: `"ЯНВ, 24"`.
*   **Кастомный:** Один интервал от `fd[0]` до `fd[1] + 1 день`. Метка: `"01/01/2024 - 15/01/2024"`.

### Интерактивность (Drill-down)
Графики поддерживают взаимодействие. В конфигурации `chartOptions` реализован обработчик `onClick`:

*   **Клик по столбцу:** Определяется выбранный интервал (`dIndex`) и стадия (`id`).
*   **Визуальная обратная связь:** Выбранный столбец меняет цвет с зеленого на голубой (`CHART_COLORS.lightblue`).
*   **Список пользователей:** Справа от графика появляется прокручиваемый список пользователей, которые сформировали эту статистику.
*   **Повторный клик:** Клик по пустой области графика сбрасывает выделение.

#### Карточка пользователя в списке
Каждая карточка содержит:

*   **Аватар:** Компонент `Avatar` с индикатором рейтинга (закладка: зеленая/желтая/красная).
*   **Имя:** Нормализованное имя (`nameNormalization`). Неактивные пользователи отображаются с пониженной прозрачностью и красным цветом.
*   **Иконка Telegram:** Если есть `link_telegram`, отображается кликабельная иконка.
*   **Счетчик встреч:** Если `meetings_amount > 0`, показывается бейдж с числом.
*   **Компания:** Название компании или прочерк.
*   **Кнопка перехода:** Открывает профиль пользователя в новой вкладке (`window.open`).

> **Примечание:** Список пользователей формируется функцией `createUsersList`, которая сортирует пользователей по имени в алфавитном порядке.

### Библиотеки
*   **Chart.js**: Основная библиотека визуализации (`chart.js/auto`).
*   **chartjs-plugin-datalabels**: Плагин для отображения точных значений прямо на столбцах и точках графиков. Регистрируется глобально через `ChartJS.register(ChartDataLabels)`.
*   **@kurkle/color**: Утилита для работы с цветами (функция `transparentize` для создания полупрозрачных версий цветов).
*   **date-picker-svelte**: Компонент `DateInput` для выбора дат в кастомном режиме. Локализован на русский язык.

---

## Особенности и ограничения

### Производительность
*   ⚠️ **Heavy Client Logic:** Вся фильтрация и агрегация происходит в браузере. При большом объеме данных (тысячи пользователей и логов) возможны задержки при переключении фильтров.
*   **Оптимизация:** Используется дедупликация через кэш-объекты для исключения повторного подсчета одного пользователя в интервале.
*   **Рекомендация:** При росте базы рассмотреть перенос агрегации на сервер (Server-Side Aggregation).

### Особенности UI
*   **Адаптивность:** Графики адаптируются под размер экрана. На мобильных устройствах список пользователей отображается под графиком, а не справа.
*   **Автомасштабирование:** Ось Y графиков автоматически подстраивается под максимальное значение + 20% запаса.
*   **Отключение tooltip:** Для детальных графиков по стадиям tooltip отключен (`enabled: false`), вместо этого используется `datalabels` для отображения значений.

### Технический долг
*   **Дублирование кода:** Карточки пользователей рендерятся инлайн для каждого типа графика (current, custom, stages). Изменения UI нужно вносить в 3 места.
*   **Жестко закодированные стадии:** Массив `stages` и цвета `CHART_COLORS` зашиты в компоненте. При изменении бизнес-логики стадий потребуется обновление кода.
*   **Отсутствие пагинации:** Список пользователей справа от графика не имеет пагинации. При клике на популярную стадию может отобразиться сотни карточек.

### Доступ
Экран доступен пользователям с ролями: `admin`, `manager`, `moderator`, `chief`. Выбор менеджера в фильтре отображается только для этих ролей.