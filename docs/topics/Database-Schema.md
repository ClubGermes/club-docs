# Схема базы данных

Проект использует **PostgreSQL** версии 15.

> **💡 Совет:**
> Для изучения схемы базы данных настоятельно рекомендуется использовать IDE, такую как **DBeaver**, **DataGrip** или **Postico**.
> Подключитесь к `localhost:5433` (пользователь `club_user`, пароль `club_password`, БД `club_db`) и сгенерируйте ER-диаграмму автоматически.

## Основные схемы (Schemas)

В базе данных используется логическое разделение по схемам:

| Схема | Описание | Основные таблицы |
|-------|----------|------------------|
| `public` | Legacy-таблицы монолита | `users`, `events_users`, `crm_*` |
| `users` | Данные пользователей (New) | `user_profile`, `auth_logs` |
| `events` | События (New Architecture) | `event`, `speaker`, `registration` |
| `media` | Метаданные файлов | `file`, `image_variant` |

## Ключевые связи (Legacy)

Так как `club-api` работает с сырым SQL, связи не всегда очевидны (нет Foreign Keys на уровне ORM).

*   **Users <-> Events:** Таблица `events_users` связывает `user_id` и `event_id`.
*   **Users <-> Roles:** Роли часто хранятся в битовой маске или массиве строк в таблице `users` (поле `roles` или `permissions`).