# 🔍 Менеджер: Инспекция (Контроль качества)

**Код:** `Inspection.svelte` (frontend/club-manager/src/views/Inspection.svelte)  
**Роут:** `/inspection` (Layout: `Main`)

## Назначение
Экран предназначен для контроля качества работы с клиентами и выявления проблемных ситуаций. Автоматически определяет пользователей, требующих внимания менеджера на основе заданных критериев (просрочки, отсутствие активности, недостаток встреч). Визуализирует путь каждого пользователя через воронку продаж за последние 30 дней.

> **Визуализация UI**

![Inspection Dashboard](../images/manager/inspection/manager-inspection-main.png)
{style="block"}

---

## Элементы интерфейса

### Панель фильтров
Управляет выборкой данных для инспекции.

#### Выбор менеджера
Выпадающий список для фильтрации по конкретному сотруднику.

*   **Все менеджеры** (`id: -1`): Показать всех клиентов.
*   **Нет менеджера** (`id: 0`): Клиенты без назначенного менеджера.
*   **Список менеджеров**: Динамически загружается из `data.community_managers`. Для менеджеров Отдела Продаж добавляется метка `(ОП)`.

#### Выбор агента
Выпадающий список для фильтрации по агенту (рефералу).

*   **Все агенты** (`id: -1`): Показать всех клиентов.
*   **Нет агента** (`id: 0`): Клиенты без агента.
*   **Список агентов**: Динамически формируется из данных пользователей. Сортируется по алфавиту.

#### Тип менеджера (Тогглы)
Круглые кнопки-фильтры для быстрого переключения отображения по департаментам:

*   **КМ** (Синий, `border-secondary`): Community Manager (department = 1).
*   **ОП** (Бирюзовый, `border-info`): Отдел Продаж (department ≠ 1).
*   **НЕТ** (Красный, `border-error`): Пользователи без назначенного менеджера.

#### Обновить
Кнопка с иконкой обновления в правом верхнем углу. Принудительно перезагружает данные с сервера через `get()`.

---

### Блок "Собеседования" (Interviews)

**Отображается:** Только если есть запланированные собеседования.

Список пользователей с запланированными собеседованиями, загружается из массива `interviews`.

#### Структура карточки
*   **Аватар:** Компонент `Avatar` с индикатором рейтинга.
*   **Имя:** Нормализованное имя пользователя.
*   **Иконка Telegram:** Если есть `link_telegram`.
*   **Счетчик встреч:** Если `meetings_amount > 0`.
*   **Компания:** Название компании или прочерк.
*   **Дата и время собеседования:** 
    *   Формат: `DD/MM hh:mm` (например: "26/01 15:30").
    *   Цвет: **Желтый** (warning) если собеседование в будущем, **Красный** (error) если просрочено.
*   **Комментарий:** Текст из `comment_interview`.

---

### Легенда стадий

Горизонтальная панель с цветными индикаторами всех стадий воронки (0-6). Помогает расшифровать цвета на временных шкалах.

---

### Блоки контроля качества

Семь автоматических проверок, каждая в отдельном блоке с синим заголовком.

#### 1. Соискатель 3 дн.
**Критерий:** Находятся на стадии «Соискатель» более 3 рабочих дней.

**Логика:**
```typescript
u.membership.stage == 1 && t3 > log[log.length - 1].time
```
Где `t3` — дата 3 рабочих дня назад (исключая выходные из `WORK`).

#### 2. Адмиссия 3 дн.
**Критерий:** Находятся на стадии «Адмиссия» более 3 рабочих дней.

**Логика:**
```typescript
u.membership.stage == 2 && t3 > log[log.length - 1].time
```

#### 3. Кандидат 3 дн.
**Критерий:** Находятся на стадии «Кандидат» более 3 рабочих дней и не имеют состоявшихся или запланированных онлайн-встреч.

**Логика:**
```typescript
u.membership.stage == 4 && 
t3 > log[log.length - 1].time && 
!u.meetings_amount
```

#### 4. Кандидат 14 дн.
**Критерий:** Находятся на стадии «Кандидат» более двух недель и имеют менее 5 состоявшихся или запланированных онлайн-встреч.

**Логика:**
```typescript
u.membership.stage == 4 && 
t14 > log[log.length - 1].time && 
u.meetings_amount < 5
```

#### 5. Собеседование
**Критерий:** Находятся на стадии «Кандидат» более 5 рабочих дней и не прошли собеседование.

**Логика:**
```typescript
u.membership.stage == 4 && 
t5 > log[log.length - 1].time && 
!stateInterview
```
Где `stateInterview` берется из `u.membership.semaphore` с `id == 1`.

#### 6. Встречи <10
**Критерий:** Находятся на стадии «Кандидат», имеют менее 10 состоявшихся онлайн-встреч и не имеют запланированных онлайн-встреч в течение 3 рабочих дней.

**Логика:**
```typescript
u.membership.stage == 4 && 
u.meetings_finished_amount < 10 && 
u.meetings_planned_log.filter(t => t < f3).length < 1
```
Где `f3` — дата через 3 рабочих дня в будущем.

#### 7. Встречи <5
**Критерий:** Находятся на стадии «Кандидат», имеют менее 5 состоявшихся онлайн-встреч и не имеют запланированных онлайн-встреч в течение 10 рабочих дней.

**Логика:**
```typescript
u.membership.stage == 4 && 
u.meetings_finished_amount < 5 && 
u.meetings_planned_log.filter(t => t < f10).length < 1
```

---

### Структура блока контроля

#### Заголовок блока
*   **Название:** Краткое описание проблемы (например: "Кандидат 3 дн.").
*   **Счетчик:** Количество пользователей в этой категории.
*   **Фильтры:** Три кнопки справа:
    *   **Отложен** (иконка паузы): Показать только с флагом `postopen`.
    *   **Отказ** (иконка крестика): Показать только с флагом `rejection`.
    *   **Неактивные** (красный крестик): Показать только неактивных (`active: false`).

> **Примечание:** Фильтры применяются независимо для каждого блока (массив `filterUsers[i]`).

#### Описание блока
Под заголовком отображается детальное описание критерия серым мелким шрифтом.

#### Временная шкала
Показывает период за последние 30 дней от текущей даты.

*   **Левая граница:** Дата 30 дней назад (например: "27/12").
*   **Правая граница:** Текущая дата (например: "26/01").
*   **Границы:** Отображаются пунктирными вертикальными линиями.

#### Карточка пользователя

**Левая часть (360px):**
*   **Аватар:** Компонент `Avatar` с индикатором рейтинга.
*   **Имя:** Нормализованное имя. Неактивные пользователи отображаются с пониженной прозрачностью и красным цветом.
*   **Иконка Telegram:** Если есть `link_telegram`.
*   **Счетчик встреч:** Если `meetings_amount > 0`.
*   **Компания:** Название компании или прочерк.

**Правая часть (Timeline):**
Горизонтальная полоса, разделенная на сегменты по стадиям за последние 30 дней:
*   **Цвет сегмента:** Соответствует цвету стадии из `CHART_COLORS`.
*   **Ширина сегмента:** Пропорциональна времени нахождения на стадии (в процентах от 30 дней).
*   **Серый сегмент:** Период до первого изменения стадии в рамках 30 дней.

**Особенность:** Если событие произошло раньше 30 дней назад, оно обрезается до начала периода (`scaleMin`).

---

## Бизнес-логика и Стадии

Стадии воронки идентичны другим экранам (ID 0-6):

| ID | Название | Цвет | Описание |
| :--- | :--- | :--- | :--- |
| **0** | У агента | 🔴 Красный | Лид передан агенту |
| **1** | Соискатель | ⚫️ Темно-серый | Заполнил первичную анкету |
| **2** | Адмиссия | 🟡 Желтый | Проходит проверку |
| **3** | Подключение | 🔵 Голубой | Оформление документов |
| **4** | Кандидат | 🔵 Синий | Утвержден, ожидает оплаты |
| **5** | Оплата | 🟢 Светло-зеленый | Процесс оплаты |
| **6** | Член клуба | 🟢 Зеленый | Действующий резидент |

---

## Техническая реализация

### Архитектура данных (Client-Side Processing)
Экран работает по принципу **толстого клиента** с автоматической фильтрацией.

1.  **Запрос:** Entity `userMonitorInspector` (`src/queries/user.ts`) загружает данные.
    *   **Эндпоинт:** `/ma/user/monitor/inspector`.
    *   **Параметры:** `filter` (включая `meetings_finished_amount`, `meetings_planned_log`), `showMeetingsAmount: true`.
    *   **Возврат:** `{ log: [], users: {}, community_managers: [], interviews: [] }`.

2.  **Фильтрация:** Функция `createMonitor(log)` фильтрует пользователей по критериям:
    *   Менеджер (`filter.communityManager`).
    *   Агент (`filter.agent`).
    *   Департамент (КМ/ОП/НЕТ).

3.  **Построение Timeline:**
    *   Для каждого пользователя из `cacheLog` извлекаются все события изменения стадии за последние 30 дней.
    *   События раньше 30 дней обрезаются: `tLog = scaleMin > item.time_log ? scaleMin : item.time_log`.
    *   Вычисляется ширина каждого сегмента как процент от 30 дней.
    *   Результат сохраняется в `dataUsersLog[user_id]`.

4.  **Автоматическая категоризация:** Функция `getFilteredUsers()` распределяет пользователей по 7 блокам на основе критериев.
    *   Использует данные из `WORK` (массив рабочих дней) для расчета рабочих дней.
    *   Проверяет флаги `postopen`, `rejection`, `active` для дополнительной фильтрации.
    *   Результат сохраняется в `dataUsersFiltered[i]` (массив из 7 массивов).

5.  **Реактивность:** Svelte автоматически пересчитывает данные при изменении фильтров.
    *   `$: log, filter, createMonitor(log)`
    *   `$: dataUsers, filterUsers, getFilteredUsers()`

### Расчет рабочих дней

Используется массив `WORK` из `@/data/work` — список timestamp'ов рабочих дней.

**Алгоритм расчета "3 рабочих дня назад":**
```typescript
const dt3 = new Date();
dt3.setHours(0, 0, 0, 0);
let cnt = 3;
while (cnt > 0) {
    dt3.setDate(dt3.getDate() - 1);
    const t = dt3.getTime();
    if (WORK.findIndex(d => d == t) == -1)  // Если не выходной
        cnt--;
}
const t3 = dt3.getTime();
```

Аналогично для 5, 10 рабочих дней и будущих дат (`f3`, `f10`).

### Функция getFilteredUsers

Проходит по всем пользователям (`dataUsers`) и для каждого проверяет соответствие критериям 7 блоков:

```typescript
dataUsers.forEach(u => {
    const stage = u.membership.stages[u.membership.stage];
    const log = dataUsersLog[u.id.toString()];
    
    // Блок 0: Соискатель 3 дн.
    if (u.membership.stage == 1 && t3 > log[log.length - 1].time && ...)
        temp[0].push(u);
    
    // ... остальные блоки
});
```

Дополнительно проверяются фильтры `filterUsers[i].post`, `filterUsers[i].reject`, `filterUsers[i].inactive`.

### Библиотеки
*   **@kurkle/color**: Утилита для работы с цветами (функция `transparentize`).
*   **@/data/work**: Массив рабочих дней для расчета дедлайнов.
*   **@/helpers/datetime**: Функция `formatDatetime` для форматирования дат собеседований.

---

## Особенности и ограничения

### Производительность
*   ⚠️ **Heavy Client Logic:** Вся фильтрация и категоризация происходит в браузере. При большом объеме данных возможны задержки.
*   **Оптимизация:** Используется реактивность Svelte для пересчета только при изменении фильтров.
*   **Фиксированный период:** Анализируются только последние 30 дней, что ограничивает объем обрабатываемых данных.

### Особенности UI
*   **Адаптивность:** На мобильных устройствах временная шкала может быть сжата.
*   **Независимые фильтры:** Каждый блок имеет свои фильтры "Отложен/Отказ/Неактивные".
*   **Автоматическое обновление:** При изменении данных пользователей автоматически пересчитываются все блоки.

### Технический долг
*   **Жестко закодированные критерии:** Все 7 проверок зашиты в коде. Изменение критериев требует правки компонента.
*   **Дублирование кода:** Карточки пользователей рендерятся для каждого блока с небольшими отличиями.
*   **Зависимость от WORK:** Массив рабочих дней должен быть актуальным и включать все праздники/выходные.
*   **Отсутствие пагинации:** Все пользователи в блоке отображаются сразу (может быть много).

### Доступ
Экран доступен пользователям с ролями: `admin`, `manager`, `moderator`, `chief`, `community manager`, `curator`.

**Особенности для агентов:**
*   Пользователи с ролью только `agent` видят только своих клиентов.
*   Вместо выпадающего списка агентов отображается их имя.

---

## Отличия от других экранов

| Параметр | Статистика | Конверсия | Инспекция |
| :--- | :--- | :--- | :--- |
| **Назначение** | Анализ динамики | Анализ конверсии | Контроль качества |
| **Период** | Настраиваемый | Настраиваемый | Фиксированный (30 дней) |
| **Фильтрация** | По периоду | По дате создания | Автоматическая по критериям |
| **Визуализация** | Графики | Pie Chart + Timeline | Timeline по блокам |
| **Количество блоков** | 1 экран | 1 экран | 7 автоматических блоков |
| **Собеседования** | Нет | Нет | Отдельный блок |
| **Рабочие дни** | Не учитываются | Не учитываются | Учитываются (WORK) |

---

## Примеры использования

### Сценарий 1: Проверка просрочек
Менеджер открывает экран "Инспекция" и видит:
*   Блок "Соискатель 3 дн." — 5 пользователей.
*   Блок "Адмиссия 3 дн." — 2 пользователя.

Менеджер кликает на аватар пользователя → открывается профиль в новой вкладке → принимает меры.

### Сценарий 2: Контроль встреч
Менеджер проверяет блок "Встречи <10":
*   Видит 12 кандидатов без запланированных встреч.
*   Активирует фильтр "Отложен" → список сокращается до 3 пользователей.
*   Связывается с ними для планирования встреч.

### Сценарий 3: Собеседования
Менеджер видит блок "Собеседования" с 4 пользователями:
*   2 собеседования просрочены (красный цвет).
*   2 собеседования запланированы на сегодня (желтый цвет).

Менеджер связывается с просроченными кандидатами для переноса.
